#include <stdio.h>

int main()
{
    int numero_de_lados;
    float medida_dos_lados, area;

    printf("informe o numero de lados do poligono:\n");
    scanf("%d", &numero_de_lados);

    printf("Qual a medida dos lados(em cm)?\n");
    scanf("%f", &medida_dos_lados);  

    if(numero_de_lados = 3)
    {
        printf("triangulo.");

        area = 
    }  

    return 0;
}