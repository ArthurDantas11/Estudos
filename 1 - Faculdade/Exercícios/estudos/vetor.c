#include <stdio.h>
#define elementos 5

int main()
{
    int x[elementos], y[elementos], uniao[elementos], diferenca[elementos], soma[elementos], produto[elementos], cont_uniao = 0;

    printf("preencha o vetor 'X' a seguir:\n");
    for (int i = 0; i < elementos; i++)
    {
        printf("Posicao %d: ", i+1);
        scanf("%d", &x[i]);
    }
    
    printf("preencha o vetor 'Y' a seguir:\n");
    for (int i = 0; i < elementos; i++)
    {
        printf("Posicao %d: ", i+1);
        scanf("%d", &y[i]);
    }

    for (int i = 0; i < elementos; i++)
    {
        for (int i = 0; i < count; j++)
        {
            /* code */
        }
        
    }
    


    
    return 0;
}